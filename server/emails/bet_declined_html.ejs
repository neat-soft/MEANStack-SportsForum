<h1>
    {{= by.name}} declined Yolo
</h1>
<h3>{{= by.name }} has declined a Yolo you're involved in at <a href="{{= url }}">{{= siteName }}: {{= conversationTitle }}</a></h3>
<p>
    {{ if(comment.ptext) { }}
        {{- comment.ptext }}
    {{ } else { }}
        {{= comment.text }}
    {{ } }}
</p>
<p class="bodyFooter">
    <% include burnzonefooter_html %>
</p>
                                            
