<h1>
    {{ if job.likeChanges.up <= 0 && job.likeChanges.down != 0 { }}
        {{ likedown = likeChanges.down > 0 }}
        {{ if (likedown) { }}
            Someone down-liked your comment
        {{ } else { }}
            Someone retracted the down-like for your comment
        {{ } }}
    {{ } else { }}
        {{ likeup = likeChanges.up > 0 }}
        {{ if (likeup) { }} 
            Someone liked your comment
        {{ } else { }}
            Someone retracted the like for your comment
        {{ } }}
    {{ } }}
</h1>
<h3>
    {{ if job.likeChanges.up <= 0 && job.likeChanges.down != 0 { }}
        {{ likedown = likeChanges.down > 0 }}
        {{ if (likedown) { }}
            Someone down-liked your comment
        {{ } else { }}
            Someone retracted the down-like for your comment
        {{ } }}
    {{ } else { }}
        {{ likeup = likeChanges.up > 0 }}
        {{ if (likeup) { }} 
            Someone liked your comment
        {{ } else { }}
            Someone retracted the like for your comment
        {{ } }}
    {{ } }}
    at <a href="{{= url }}">{{= siteName}}: {{= conversationTitle}}</a>
</h3>
<p>{{= text }}</p>
<strong>Likes</strong>: Up {{= no_likes }}, Down {{= no_likes_down }}
<p class="bodyFooter">
    <% include burnzonefooter_html %>
</p>
                                            
