{{ if (comment.forum) { }}
    [{{= siteName }}] New Forum Topic (moderator): {{= conversationTitle }}
{{ } else { }}
    [{{= siteName }}] New Comment (moderator) at "{{= conversationTitle }}"
{{ } }}
